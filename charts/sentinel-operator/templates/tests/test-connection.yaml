apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sentinel-operator.fullname" . }}-test-connection"
  labels:
    {{- include "sentinel-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sentinel-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
