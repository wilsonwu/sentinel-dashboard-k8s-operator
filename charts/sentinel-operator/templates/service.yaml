apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel-operator.fullname" . }}
  labels:
    {{- include "sentinel-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentinel-operator.selectorLabels" . | nindent 4 }}
